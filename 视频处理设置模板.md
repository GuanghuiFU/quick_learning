# 视频处理设置模板

> 每次请求处理视频前，请填写此模板（复制并填好你的需求）。**API key 不要在对话中明文提供**——在 `server/.env` 里配置即可（见第 10 项）。

## 视频信息

1. **视频链接或本地路径**（必填）：
   - 单个视频 URL：`https://www.bilibili.com/video/BVxxx`
   - 视频文件夹：`/path/to/videos`
   - 本地文件：`/path/to/video.mp4`

2. **课程名**（生成笔记的文件夹名，如 `大模型微调`）：

3. **是播放列表（多集）还是单集？**：单选（单集 / 播放列表）

4. **视频语言**（自动检测，如已知可填）：中文 / 英文 / 不确定

## 处理设置

5. **下载清晰度**（360 / 480 / 720 / 1080；B 站会员可下高清，自动降级）：默认 720

6. **下载方式**（auto / ytdlp / opencli）：默认 auto（yt-dlp 优先，失败 opencli 浏览器兜底）

7. **并行 worker 数**（多集/多课程时，默认 3，API 充足可 6）：

8. **生成的文档类型**（速览 / 完整笔记 / 两者都要）：默认 两者都要

9. **总结优先级**（字幕为主 / 语音为主 / 两者并重）：默认 字幕为主

## 环境确认

10. **API key 配置**（无需在此填写，确认已在 `server/.env` 配置即可）：
    - `LLM_API_KEY`（DeepSeek）：已配置 / 未配置
    - `ASR_API`（阿里云百炼 DashScope）：已配置 / 未配置
    - 未配置请先申请：DeepSeek → https://platform.deepseek.com/ ；阿里云百炼 → https://bailian.console.aliyun.com/

11. **Obsidian vault 路径**（`server/.env` 的 `VAULT_PATH`）：已配置 / 未配置

## 可选备注

12. **其他要求**（如：只要文字稿、只要速览、按某规则筛选等）：
